id: barre1-E-er
input:
  trace: traces/barre1-E-er/
maturity: provisional
tags:
  source: youtube
  teacher: yt-barre1
  count_style: step_names
  explanation: interleaved
  lang: en
expect: {}
notes: >-
  Ballet Barre 1 DEV ingestion (charter W4); execution take, right side. Traces frozen 2026-08-22, case file written 2026-08-29. Exercise identity is deliberately absent from this file: the batch is split at the exercise level and the held-out four are contained by physical absence, so agent-authored repo text carries opaque ids only.

  No `expect` labels are proposed. Tempo: this batch carries no
  metronome label, no owner-verified grid, and its media is `offrepo:`, so
  the only available BPM is the pipeline's own reading -- writing that back
  as truth would score the pipeline against itself. Meter: the teacher's
  counting in this material counts the *count phrase*, not the bar (see the
  2026-08-29 W4 pre-registration, finding F2), so "counted in eights" does
  not license 4/4 here. Counts: no run of counting covers a full eight
  anywhere in the batch (finding F1). Every one of these labels has to come
  from the piano, which means an owner-verified beat grid on media this
  runner does not hold.
